# TODO

## Pipeline
- [x] Ensure pipeline outputs normalisation files (seasonal means and stds) are generated in the data folder during the main run, ensure these are only calculated from the same training dataset used to train the model; they should be calculated on running main as on the initial run the data folder will be empty
- [ ] Investigate why the full moisture set (starting in 2012) didn't download, ensure the pipeline is fixed to get the full download and the .h5 and temporal moisture CSV are 
- [ ] Check that main.py is using the same prediction methodology as was used in training the optimal model (which main.py should be using); ensure main.py reports which model was used along with its test set performance metrics in some file in the predictions folder (if it doesn't do this already)
- [ ] Investigate what temporal_data_seasonal_df is used for and whether it makes sense in the pipeline
- [ ] Clean up the README to remove old information, link the image in the README to the most recent year-by-year comparison image from the prediction folder, if one exists (which means predictions folder should not be gitignored)
- [ ] Update README to simplify (should be clear instructions for how to run, especially for the first time, not overcomplicated information or specific information about the current best model) and remove references to external data folder, specify that the first data run will take longer as it needs to download and generate historic record for spatio-temporal datasets
- [ ] Update README to explain config file and key parameters that can be changed
- [ ] Set up model (optimal configuration) to retrain on data generated by pipeline, use that for prediction, if config retrain=True
- [ ] Print a warning at the model predictions if there is a high degree of imputation
- [ ] Ensure the feature history (per the explanation plot code, whose name should be changed) are being produced and saved
- [ ] Extract feature explanations using SHAP and save those along with the feature history per prediction
- [ ] Refactor main.py to align with the current pipeline and model selection laid out in the config, ensure temporal predictions, spatial predictions, variable history, and model explanations are all toggled on/off in the config file; remove any obsolete code; modularise and reorganise where it makes sense (e.g. storing support functions elsewhere to be cleaner) 

## Model Improvements
- [ ] Add GloFAS data
- [ ] Improve efficiency of data extraction and training for spatial model
- [ ] Retrain spatial model and validate using VIIRS data
- [ ] Train spatial model again on full data set and deploy
- [ ] Ensure all pipeline parameters are input in the config and not hardcoded